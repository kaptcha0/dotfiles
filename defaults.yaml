linux:
  installs:
    cmd: yay -S {{name}}
    depends:
      - yay
darwin:
  installs:
    cmd: brew install {{name}}
    depends:
      - brew
windows:
  installs:
    cmd: scoop install {{name}}
    depends:
      - scoop
