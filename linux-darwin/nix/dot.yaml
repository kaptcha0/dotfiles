linux|darwin:
  links:
   nix.conf: /etc/nix/nix.conf

linux:
  installs: sh <(curl --proto '=https' --tlsv1.2 -L https://nixos.org/nix/install) --daemon

darwin:
  installs: sh <(curl --proto '=https' --tlsv1.2 -L https://nixos.org/nix/install)